from setuptools import setup

setup(
   name='linkpredpy',
   version='0.1',
   description='A link prediction library',
   author='Rakan Dirbas',
   packages=['linkpredpy'],
   install_requires=['numpy', 'scipy', 'networkx']
)
